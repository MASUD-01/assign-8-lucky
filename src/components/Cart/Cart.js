import React from 'react';

const Cart = (props) => {
    const { name } = props
    return (
        <div>
            <h1>Name:{name}</h1>
            <button></button>
        </div>
    );
};

export default Cart;